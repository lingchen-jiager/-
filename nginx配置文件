server {
    listen 443 ssl http2;
    server_name xxx.yourdomin.xyz; #改你自己域名

    # SSL 配置
    ssl_certificate /www/server/panel/vhost/cert/xxx.yourdomin.xyz/fullchain.pem;   #用宝塔面板申请好域名的免费https证书（let's encrypt），这里改你自己域名
    ssl_certificate_key /www/server/panel/vhost/cert/xxx.yourdomin.xyz/privkey.pem;   #改你自己域名
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;

    # 反向代理配置
    location / {
        proxy_pass https://127.0.0.1:8384;         #宝塔设置反向代理时候，代理到8384端口
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 增加超时设置
        proxy_connect_timeout 60s;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        
        
         # 添加以下几行来传递真实IP
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    real_ip_header X-Forwarded-For;
    set_real_ip_from 127.0.0.1;
    }

    # 添加 favicon.ico 处理
    location = /favicon.ico {
        access_log off;
        log_not_found off;
        return 204;
    }
}

# HTTP 重定向到 HTTPS
server {
    listen 80;
    server_name xxx.yourdomin.xyz;   #改你自己域名
    return 301 https://$server_name$request_uri;
}